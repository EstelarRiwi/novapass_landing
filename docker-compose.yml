services:
  app:
    image: ghcr.io/estelarriwi/novapass_landing:latest
    pull_policy: always
    env_file:
      - .env
    volumes:
      - app-storage:/var/www/storage
    ports:
      - "9000:9000"
    restart: unless-stopped

volumes:
  app-storage:
